/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:
        [
            "drive.google.com",
            "images.unsplash.com"
        ]
    },
    optimizeFonts: false,
}

module.exports = nextConfig
